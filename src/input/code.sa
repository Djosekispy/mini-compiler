VAR a : INTEIRO.
VAR b = 2 + 3 * 4 : INTEIRO.
VAR c = 10 / 2 + 3 : INTEIRO.

EXIBIR(a).
EXIBIR(b).
EXIBIR(c).

VAR ola = "Olá, mundo! Foram 50 commits só hoje" : TEXTO.
EXIBIR (ola).

a=5.
SE(a != 8 E c < 10){
    EXIBIR("Verdadeiro 1").

    EXIBIR(b).
}

SENAO{
    EXIBIR("TERCEIRA OPCAO  b == 5 e  NAO(b!=2)").
}
